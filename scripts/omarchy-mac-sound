#!/usr/bin/env bash
# =============================================================================
# 🍏 macOS Sequoia Native Quickshell Sound Dispatcher
# =============================================================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CONFIG_DIR="$HOME/.config/omarchy-undercover"

QS_SRC=""
for cand in \
    "$CONFIG_DIR/quickshell/mac-sound" \
    "$CONFIG_DIR/configs/quickshell/mac-sound" \
    "${SCRIPT_DIR}/../quickshell/mac-sound" \
    "${SCRIPT_DIR}/../configs/quickshell/mac-sound" \
    "${SCRIPT_DIR}/quickshell/mac-sound" \
    "${SCRIPT_DIR}/configs/quickshell/mac-sound"; do
    if [[ -d "$cand" ]]; then
        QS_SRC="$cand"
        break
    fi
done

if pgrep -f "quickshell.*mac-sound" >/dev/null; then
    pkill -f "quickshell.*mac-sound"
    exit 0
fi

if command -v quickshell >/dev/null 2>&1 && [[ -d "$QS_SRC" ]]; then
    exec quickshell -p "$QS_SRC"
else
    exec pavucontrol
fi
