# Input & Mouse Behavior
input {
    follow_mouse = 1
    mouse_refocus = true
}

misc {
    focus_on_activate = true
    mouse_move_enables_dpms = true
    animate_manual_resizes = true
}

# General Layout
general {
    gaps_in = 4
    gaps_out = 8
    border_size = 1
    col.active_border = rgba(0078d4ff) rgba(00b7c3ff) 45deg
}

# Decoration
decoration {
    rounding = 8
    blur {
        enabled = true
        size = 8
        passes = 2
        vibrancy = 0.2
    }
    drop_shadow = true
    shadow_range = 20
    col.shadow = rgba(00000055)
}

# Animations
animations {
    enabled = yes

    bezier = fluent, 0.25, 1, 0.5, 1

    animation = windowsIn, 1, 4, fluent, slide
    animation = windowsOut, 1, 4, fluent, slide
    animation = workspaces, 1, 4, fluent, slide
}

# Keybindings for Windows 11 Mode
bindr = SUPER, SUPER_L, exec, omarchy-undercover-launcher
bind = SUPER, d, exec, omarchy-undercover-launcher
bind = SUPER, TAB, exec, rofi -show window -theme ~/.config/omarchy-undercover/rofi/windows11.rasi
bind = ALT, F4, killactive,
bind = SUPER, e, exec, env GTK_THEME=Adwaita:dark GTK_ICON_THEME=Fluent-dark nautilus --new-window computer:/// || thunar || nemo || dolphin || pcmanfm
bind = SUPER, l, exec, hyprlock || swaylock || loginctl lock-session
bind = SUPER, left, movewindow, l
bind = SUPER, right, movewindow, r
bind = SUPER, up, fullscreen, 1
bind = SUPER, down, togglefloating,
